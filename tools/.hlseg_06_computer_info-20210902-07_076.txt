076、月結把各分店工作表貼進總表時若沒先清掉上次結果，第二次執行會把舊列再疊一次，合計會加倍。
077、跨表複製若沿用錄製下來的絕對位址，來源多一列空白就貼錯列，應先找來源最後一筆非空列，再對目的表最後一列的下一列寫入。
078、各單位欄位順序不同時整列複製會把金額貼進數量欄，彙整前應用欄名對位，不能依欄號硬貼。
079、隱藏列與群組摺疊列仍會被 Copy 帶走，畫面列數變少但合計對不上時，應先解除隱藏或只複製可見儲存格。
080、來源含合併儲存格時貼到未合併的總表常出現 1004，應先取消合併再取值，或只讀左上角儲存格。
081、來源已有小計列又整段複製，總表 SUM 會把小計再加一次，應只抓明細，或複製後刪掉含「合計」字樣的列。
082、從關閉的活頁簿取值比一次開十個檔再切換作用中活頁簿穩，但路徑寫死時同事一改資料夾名稱整批就失敗。
083、篩選後只複製可見列另存新檔，若結果為零筆，SpecialCells 會丟錯，應先判斷可見列數再決定是否建檔。
084、AutoFilter 用字串「2021/9/1」當等於日期，在年/月/日地區設定下可能篩不到，應以 DateSerial 組成真正日期再當條件。
085、進階篩選可一次把多條件結果寫到另一張表，比逐列 If 快，但條件區與輸出區重疊會洗掉原資料。
086、篩完匯出 CSV 給會計系統時，長發票號碼會被改成科學記號，該欄應先設成文字或加單引號再存。
087、複製可見列時若沒關螢幕更新，大量篩選會閃爍變慢；關閉 ScreenUpdating 後必須在錯誤處理裡恢復，否則畫面像當機。
088、把篩選結果另存後若原檔沒取消篩選，下次開啟會以為資料變少，匯出結尾應 ShowAllData。
089、跨工作表用剪貼簿複製時，中途若跳出訊息框讓人停很久，其他程式可能清掉剪貼簿，應改以 Range.Value 對拷。
090、目的表已是清單物件時，貼到表格外會斷開結構化參照，應貼進表格最後一列讓它自動擴張，不要自己猜下一列。
091、工作表受保護時複製會 1004，程序開頭 Unprotect、結尾再 Protect，密碼不要用明文寫在模組裡給全公司看。
092、彙整多張月報若每張都 Select 再 Copy，遠端桌面會卡；改對物件賦值並一次寫入陣列，比儲存格迴圈快得多。
093、日期欄有的是真正日期、有的是八碼文字「20210901」，CDate 遇無分隔常失敗，應先判斷長度與 IsDate，再決定用 DateSerial 拆年月日。
094、用 Format 把日期轉成「yyyy年m月」當工作表名稱時，若含斜線或冒號會無法新增工作表，應先取代非法字元。
095、DateAdd 加一個月碰到 1 月 31 日會落到 2 月 28 或 29，用來當「下月同期」會少一天，月底對月底應改取該月最後一天。
096、部門名後面若有全形空白，一般 Trim 清不掉，篩選會漏列，應再 Replace 全形空白或自訂清空白。
097、InStr 預設區分大小寫，英文品名 SKU 與 sku 對不到，模糊比對要用文字比較模式，中文全半形仍須另外正規化。
098、Split 用逗號拆關鍵字，資料若用頓號分隔會整段當一個詞，匯入前應統一分隔符，不能假設都是半形逗號。
099、字串截取只能清格式，不能當合法性檢查；身分證或統編仍要查長度與字元種類，不要只靠 Left 或 Mid。
100、Replace 拿掉連字號來統一統編時，若連日期「2021-09-01」一起替換，會黏成數字被當成序號，應限定欄位再替換。
101、錄製巨集會把選取、捲動、色彩全記下來，貼到介面語言不同的 Excel 時部分名稱對不上，交付前應刪掉 Select 與捲動。
102、相對參照錄製時若作用儲存格不在預期起點，巨會整段偏移，給別人按按鈕前應固定起點或改成對物件寫入。
103、錄製「尋找」會把查找範圍與比對方式留在 Excel 全域狀態，之後使用者手動尋找行為會變，用完應把參數設回常用值。
104、錄製「另存新檔」會寫死磁碟機與使用者資料夾，換電腦就路徑不存在，應改用活頁簿所在目錄或讓使用者選資料夾。
105、錄製篩選會記下當時的欄位索引，中間插入一欄後條件會套到錯欄，應改用欄名找欄號再篩，不要直接交付未改寫的錄製碼。
106、錄製圖表與樞紐的程式又長又脆，欄位一改就爆；圖表與樞紐適合手動或資料模型，程序只負責把乾淨資料鋪好。
107、個人巨集活頁簿裡的錄製碼只存在自己電腦，寄出含巨集的檔不會附上，部門共用應把程序放到該活頁簿模組。
108、信任中心設成停用所有巨集且不通知時，按鈕看起來在卻完全沒反應，部署前應確認原則允許啟用已簽署或受信任位置的巨集。
109、另存成 xlsx 會默默去掉所有 VBA，隔天開啟按鈕失效卻沒有錯誤訊息，含巨集的範本必須維持可執行巨集的檔案格式。
110、從網路或即時通訊下載的檔會被標成來自網際網路，未先解除封鎖就啟用巨集，系統仍可能擋 ActiveX 與程式碼。
111、開啟活頁簿就自動彙整並覆寫總表，誤開舊檔也會洗掉正式資料，開啟事件只適合作檢查，批次寫入應改按鈕並先備份。
112、關掉警示後存檔覆寫不再詢問，若錯誤中途跳出而未設回，之後使用者存檔都不警示，錯誤處理必須成對恢復。
113、關掉事件可避免變更事件遞迴，但忘了開回去，之後輸入都不觸發相關檢查，看起來像功能壞掉。
114、忽略錯誤會蓋住「工作表不存在」，迴圈繼續寫到錯誤物件，月底才發現缺一個分店，缺表應變成明確訊息並停止該筆。
115、1004 常來自超出範圍或工作表受保護，訊息應寫明哪一張表、哪一個動作，不要只丟原始錯誤讓現場無法處理。
116、下標超出範圍多半是工作表被改名或刪除，開頭應檢查名稱是否還在，找不到就列出目前工作表名稱供核對。
117、型別不符常出現在空儲存格做數學或日期文字混入，讀值前應分流空白、數字與日期，不要直接把整欄轉成數值。
118、Integer 加總超過 32767 會溢位，列號與筆數應用長整數，金額用倍精確或通貨型別，不要用短整數記大量張數。
119、篩選後沒有可見儲存格時 SpecialCells 會丟錯，這是條件太嚴不是檔案壞掉，應提示本條件無資料，不要進偵錯模式。
120、檔案已被同事開啟時可能變成唯讀，寫回會失敗或另存複本，開檔後應檢查唯讀狀態，是唯讀就停止並說明誰鎖檔。
121、網路磁碟斷線時另存中途失敗會留下半套檔，批次匯出應先寫本機暫存再搬移，失敗時保留原檔不要刪來源。
122、迴圈裡逐格清除會觸發每次重算，十萬格會跑數分鐘；應一次清掉目標範圍，或暫改手動計算、結束再算一次。
123、刪列時從上往下刪，後面列號上移會漏刪，應從最後一列往上刪，或先做標記再一次刪除連續區。
124、假設作用工作表一定是總表，若當時停在說明頁，貼上會蓋掉說明；按鈕程序應寫死工作表物件，不要靠目前選取的表。
125、多檔彙整若未關閉已開的來源檔，重複執行會碰到檔已開啟或讀到未存盤版本，跑完應關閉來源且不存，避免把改動寫回分店檔。
126、公式填得動的欄不要用程序逐列算，讓公式隨表格擴充較好維護；程序留給跨檔、篩選匯出與重複點擊才划算。
127、樞紐分析表就能依部門加總時不必寫迴圈加總，程序適合重新整理樞紐與改資料來源，不適合重造一套加總引擎。
128、每月版面相同的多檔合併，用取得與轉換較能在欄位微變時調整；版面每月大改又要彈窗挑檔，才輪到對話方塊與程序。
129、一次性整理幾十列用快速填滿或尋找取代就夠，寫巨集的測試時間比手動長，沒有下個月還要再跑的理由就不要自動化。
130、公司已禁用巨集或只能開不含巨集的活頁簿時，再漂亮的按鈕也無法給現場用，應改公式範本或走核准過的流程工具。
131、需要留下誰在何時改金額的稽核紀錄時，程序改儲存格不會自動產生可靠日誌，應走表單系統，或寫到獨立稽核表且禁止使用者刪。
132、多人同時開同一份總表做寫入會後寫蓋前寫，共用活頁簿模式又不穩，應一人跑批次，或各分店交檔、主檔只讀再彙整。
133、金額、稅額、匯率涉及對外報表時，程序跑完仍應抽樣核對筆數與加總，沒有報錯不代表可以寄出，自動化省的是搬運不是覆核。
134、路徑、稅率、部門對照放在設定工作表，改流程時不用進編輯器，現場比較敢用；寫死在程式裡的常數一改就要找會程式的人。
135、錯誤發生時若已寫了一半總表，應把總表標成本次未完成或還原備份，避免會計以為檔是完整的而拿去申報。
136、跑批次前用複本備份且檔名含時間戳，不要覆寫同一個備份檔，否則連續失敗兩次就沒有最早的好檔。
137、測試應先複製一份在本機跑，確認列數、加總、日期再換正式路徑；直接在伺服器正檔試跑，一次路徑錯誤就可能清掉正式資料。
138、訊息框問是否覆寫總表，在午夜排程沒人按會整支掛住，無人值守應改用設定表旗標，不要用對話框當流程開關。
139、中文路徑與檔名在部分舊介面會亂碼，目錄搜尋找不到檔卻不是真的沒檔；開檔應使用完整路徑字串，少用依賴系統字碼頁的呼叫。
140、轉出給銀行的文字檔若編碼不對，中文會變問號，應先指定對方系統可讀的編碼並用小檔試讀，不要月底一次出大檔才發現。
141、週期報表取該月最後一天可用年與月組出下月 0 日，比自己記大月小月可靠；12 月跨年時不要把月份加成 13。
142、用固定字元數比對檔名，三個字會過、四個字就漏，檔名規則一變就漏檔，應掃資料夾再比對關鍵字，不要假設檔名長度不變。
143、把部門名拼進條件字串時，名稱含單引號會截斷條件甚至造成意外指令，組字串前應把單引號加倍，能分開傳遞就不要自己拼。
144、把結果用郵件寄出時若附件仍是篩選中的原檔，收件者看到的列數取決於他會不會取消篩選，應寄只含結果的複本。
145、匯出 PDF 或列印前若不固定列印範圍與方向，現場會缺欄；程序可鎖定版面，但印表機不同時邊界仍可能裁切，重要報表應先預覽一頁。
146、條件式格式能標示逾期就不要用程序掃每一列上色，否則資料一改顏色不會自動更新，維護成本比規則高。
147、下拉清單就能限制部門時，不必靠變更事件擋輸入，因為貼上會略過事件；應以資料驗證為主，程序只做貼上後的批次檢查。
148、64 位元 Excel 跑舊的 32 位元宣告會失敗，公司升級後整份自動化可能停擺，部署前應確認宣告與環境，不要只在自己電腦錄製。
149、同一個按鈕既彙整又寄信，測試彙整時可能誤寄客戶，應把產生檔與寄出分開，正式寄出前顯示筆數、金額與收件者供確認。
150、每月重複、規則穩定、輸出格式固定且現場願意啟用巨集時才值得寫程序；欄位每週改、只要轉一次、或資訊單位禁巨集時，改用查詢、樞紐或人工核對比較不容易出事。
